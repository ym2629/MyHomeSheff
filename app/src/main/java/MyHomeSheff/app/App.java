/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package MyHomeSheff.app;

import MyHomeSheff.list.LinkedList;

import static MyHomeSheff.utilities.StringUtils.join;
import static MyHomeSheff.utilities.StringUtils.split;
import static MyHomeSheff.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
